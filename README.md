# Visual Studio Code Linting Performance Comparison

This repository is a fork of the Visual Studio Code project used to compare the performance of different linting configurations. To run the benchmark:

```bash
npm install --ignore-scripts
node --experimental-strip-types compare.ts
```
